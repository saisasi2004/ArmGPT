# ArmGPT - pinned dependencies (all open-source, no paid APIs)
# Python 3.10–3.12 (mediapipe has no 3.13 wheels yet). Verified on 3.11.
numpy==1.26.4            # <2 required by this mediapipe release
opencv-python==4.10.0.84 # includes cv2.aruco since 4.7 (no contrib build needed)
ultralytics==8.3.49      # YOLOv8n object detection (pulls in torch)
mediapipe==0.10.14       # hands + face detection
protobuf==4.25.3         # mediapipe is incompatible with protobuf >=5
pyzbar==0.1.9            # QR/barcode fallback (needs zbar DLL; bundled on Windows)
Flask==3.0.3
pymongo==4.8.0
requests==2.32.3
